# -*- mode: snippet -*-
# name: statelessWidgets
# key: slw
# --
class ${1:name} extends StatelessWidget {
  const ${1:name}({Key key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      child: ${2:child},
    );
  }
}